# A config that demonstrates basic service options.
routers:
- protocol: http
  label: foobar
  dtab: |
    /svc/* => /$/inet/localhost/8080
  service:
    kind: io.l5d.fs
    serviceFile: linkerd/examples/io.l5d.fs/service.yaml
  servers:
  - port: 4140
