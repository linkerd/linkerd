# HTTP/2 protocol.
admin:
  shutdownGraceMs: 30
routers:
- protocol: h2
  dtab: |
    /srv => /$/inet/127.0.0.1/7777;
    /svc/* => /srv;
  servers:
  - port: 4142
    maxConcurrentStreamsPerConnection: 300

